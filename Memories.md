# Boutique Rumble: Architectural Memory

## Project Ethos
- **Paradigm**: Boutique (Curated constraints over parameter density).
- **Core Aesthetic**: Rhythmic "Rumble" bass (Skrillex/Fred Again style).
- **Primary Interface**: The "Boutique 5" Macros.

## Technical Decisions Log
- **[2026-04-17] Build System**: Migrated to CMake 4.3 + JUCE 8. Established modular `Source/` subfolder structure.
- **[2026-04-17] VST3 Compatibility**: Set `JUCE_VST3_CAN_REPLACE_VST2=0` to bypass legacy automation errors.
- **[2026-04-17] Anti-Aliasing**: Decided on a hybrid approach—Poly-BLEP for Oscillators, Oversampling (2x/4x) for the GRIT/Saturation block.
- **[2026-04-17] Parameter IDs**: Centralized macro parameter IDs in `Source/Parameters/ParamConstants.h` and refactored `PluginProcessor` APVTS layout to consume constants instead of hardcoded strings.
- **[2026-04-17] Testing Infrastructure**: Added `BoutiqueTests` executable via CMake `FetchContent` with Catch2 v3 and validated setup with a dummy arithmetic test.
- **[2026-04-17] Functional Signal Path**: Transitioned from silent stubs to a working core path—Poly-BLEP morphing oscillator (sine/square/saw), 3-voice `RumbleEngine` (1 sub + 2 mids), and processor integration mapping `SHAPE`/`HARMONY` APVTS macros into live audio output.
- **[2026-04-17] Transition to Test-Driven DSP Development**: Replaced placeholder checks with waveform-accurate oscillator tests (shape boundaries at 0.0 and 0.5) and engine-level summing/prepare propagation tests, making DSP behavior verifiable by default before feature expansion.
- **[2026-04-17] Monophonic MIDI Playability with Deterministic Phase Reset**: Added note-gated oscillator activation, `noteOn`/`noteOff` management in `RumbleEngine`, processor MIDI parsing (host + on-screen keyboard), deterministic phase reset on note start, and initial `GRIT` tanh soft-clipping with make-up normalization plus safe default master gain staging.
- **[2026-04-17] Code-driven to GUI-driven Control**: Added a custom `BoutiqueLookAndFeel`, introduced five rotary APVTS-bound macro knobs (`PULSE`, `SHAPE`, `GRIT`, `GIRTH`, `HARMONY`), and laid out dashboard controls above the integrated MIDI keyboard for direct performance/UI workflow.
- **[2026-04-17] Rhythmic Gating + Slew-based Click Reduction**: Implemented host-synced 16th-note gating in `RumbleEngine` using transport BPM/PPQ, mapped `PULSE` to both gate duty cycle and slew softness, and added a dedicated `SlewLimiter` utility + verification test to ensure non-instant transitions.
- **[2026-04-17] Internal Standalone Clock Fallback**: Added processor-level internal PPQ/BPM fallback when host transport is absent or stopped, continuously driving `RumbleEngine` gate sync in Standalone mode; also added an optional standalone-only `PULSE` influence (+/-10%) over internal tempo for playful rhythmic variation.
- **[2026-04-17] Linkwitz-Riley Crossover + GIRTH Spatial Management**: Added a 150 Hz 24 dB/oct Linkwitz-Riley split (LP/HP per output channel), enforced mono low-band integrity, and applied GIRTH-driven mid/side widening to the high band before recombining for stable low-end with controllable stereo width.
- **[2026-04-17] HARMONY Frequency Ratio Mapping**: Implemented ratio-driven harmonic/inharmonic mapping in `RumbleEngine` where Sub stays at base `f`, MidA/MidB move through `2f/4f` -> `3f/4f` -> metallic ratios `2.137f/3.1415f`; frequency updates remain phase-continuous via oscillator frequency updates without forced phase jumps.
- **[2026-04-17] Transition to Variable Rhythmic Subdivisions**: Added a subdivision-aware gate clock path (`1/8`, `1/8T`, `1/16`), standalone BPM number-box control, and UI-driven division selector linked to `RumbleEngine::setSubdivision`, while keeping `PULSE` focused on duty + slew behavior and validating transition-rate changes in tests.
- **[2026-04-17] Expansion to Boutique 6: Dedicated Discrete RATE Control**: Promoted rhythmic subdivision to an automatable APVTS macro (`RATE`) with ten musical divisions (`1/1`..`1/64`), mapped to engine clock multipliers (`0.25`..`16.0`), and upgraded the UI to a sixth hard-set rate control while preserving standalone BPM and existing pulse feel.
- **[2026-04-17] UI Refinement (Rotary RATE) + High-Band Decorrelation**: Switched `RATE` from utility +/- buttons to a Boutique rotary drag control with discrete snapping via APVTS choice mapping, tightened six-knob centering, and added a short right-channel high-band delay decorrelator so GIRTH widening produces more audible stereo spread on full-range systems.
- **[2026-04-17] Implementation of 5ms Linear Release De-Click**: Replaced immediate note-off muting with a sample-accurate 5ms linear release envelope (`mNoteGainEnvelope`) and deferred oscillator deactivation until fade completion, eliminating abrupt release pops while retaining CPU-saving voice shutdown.
- **[2026-04-17] Digital Entropy + White Noise Tearing in GRIT Manifold**: Extended GRIT beyond saturation by mapping it to an internal entropy stage that adds post-0.5 bit reduction plus amplitude-tracked white-noise tearing before tanh, creating a more aggressive 'tear' texture while preserving level control and test coverage for noise-driven non-periodicity.
- **[2026-04-17] Pushing GRIT to 3-bit destruction + LFO Morphing**: Increased high-GRIT crushing range to reach ~3-bit quantization at max, introduced kinked macro tapering for `SHAPE`/`GRIT` (quadratic low-half + steeper upper ramp), and morphed low-`PULSE` gating into smooth parabolic/sine sidechain swells that transition into square gating above the threshold.
- **[2026-04-17] Transition to Morphable Rhythmic LFO (Sine/Triangle/Square)**: Updated the PULSE manifold to morph through ducking-sine -> triangle-pluck -> square gate regions while retaining slew smoothing, and added midpoint-curve verification to ensure low-pulse behavior is no longer a square plateau.
- **[2026-04-17] Implemented MIDI Note Stack for Monophonic Legato**: Added active-note tracking in `PluginProcessor` to preserve last-note priority on note releases (`A on -> B on -> A off` keeps `B` sounding), only triggering engine release when the stack is empty; included legato sequence verification in engine tests.
- **[2026-04-17] Implemented 25ms Performance Glide for Legato Growls**: Added `LinearSmoothedValue` base-frequency glide in `RumbleEngine` so legato note transitions ramp over 25ms without phase reset, while first-note attacks still snap immediately with deterministic phase start.
- **[2026-04-17] Fix for Legato Phase Discontinuity + Frequency-Based Phase Cancellation**: Hardened legato continuity by explicitly preserving oscillator phase on sustaining note changes, smoothed `SHAPE`/`GRIT` macros with `LinearSmoothedValue` to prevent automation zipper-clicks, and refined high-band decorrelation to remain strictly crossover-bound with GIRTH-controlled delay blending.
- **[2026-04-17] Hardened Phase Continuity to eliminate Legato Clicks**: Removed legato-time gate reset impulses, kept strict no-reset oscillator phase behavior for sustaining note changes, and re-routed GIRTH decorrelation so Haas widening is applied to the mid/high branch while the sub lane remains strictly mono.
- **[2026-04-17] Legato Click/Thinness Finalization Pass**: Removed remaining legato gate snap behavior, added ratio smoothing for harmonic transitions, and moved GIRTH Haas action to the >400Hz air band so 150-400Hz mids and sub fundamentals stay solid during semitone legato movement.
- **[2026-04-17] Pro-Click Hunt Hardening Pass**: Enforced lingering-signal legato behavior (no phase reset unless envelope is fully silent), added host-phase micro-jitter rejection around internal gate phase, and pinned GRIT quantization/tear to smoothed GRIT values to eliminate block-edge entropy clicks.
- **[2026-04-18] Hardcoded 20% Random Step-Skipping for rhythmic syncopation**: On each gate-phase wrap, `RumbleEngine` rolls `entropyRandom` (~20% chance) to set `mCurrentStepIsSkipped`; while set, `getGateTarget()` returns silence for that rhythmic cycle so roughly one in five pulses drops out for syncopated rumble.
- **[2026-04-18] Added dynamic SKIP slider (0-100%) for real-time rhythmic chaos control**: APVTS float `skip_prob` / UI label `SKIP` (default 0.2) drives `RumbleEngine::setSkipProbability`; at 0 the gate never skips, at 1 every wrapped step is skipped (`nextFloat() < 1`) for full rhythmic silence through the pulse.
- **[2026-04-18] Implemented Isolated Sub-Drift (3:7:11) mapped to the GIRTH knob**: Three free-running sine LFOs at 0.31 / 0.73 / 1.13 Hz (prime-scaled “3:7:11” character) sum to a normalized depth; `GIRTH` scales fine detune of Mid A/B up to ±20 cents via `2^(cents/1200)`, while mid oscillators keep advancing phase when inactive so short gates sample evolving drift.
- **[2026-04-18] Implemented Legato frequency smoothing and Gate-Dip re-triggering to eliminate note-change clicks**: `noteOn` only uses `LinearSmoothedValue::setTargetValue` for pitch when a voice is already active; legato stacked notes trigger a ~1 ms gate-target dip to zero (via existing `gateSlew` state, no `reset`) before the LFO target returns; `PULSE` slew times are clamped to at least 1 ms so square edges never use a true 0 ms transition.
- **[2026-04-18] Implemented 25Hz Safety Rail and DC Blocker to preserve infrasonic character while protecting hardware**: After GRIT/spatial summing and note envelope, each channel runs a 5 Hz first-order DC blocker then a 4th-order Linkwitz–Riley high-pass at 25 Hz so sub-audible fundamentals (e.g. C-2 ~4 Hz) are attenuated while harmonics and motion from upstream saturation remain audible.
- **[2026-04-18] Adjusted release curves to 100ms exponential to eliminate infrasonic release pops**: Note envelope release is exponential (`envelope *= exp(-1/(fs·τ))`) with ~35 ms base τ scaled ~±55% by MIDI note (longer τ for low notes); attack stays ~5 ms linear. The 25 Hz safety high-pass runs before the 5 Hz DC blocker so the DC stage is last and catches release thumps.
- **[2026-04-18] Implemented 45ms exponential Pitch-Thump (+36st) for percussive note-on transients**: Fresh `noteOn` seeds a +36 semitone offset that decays multiplicatively to ~0 in exactly 45 ms (matched to sample rate); legato retriggers cap at +18 st. Effective base Hz is `f * 2^(thump/12)` before Sub-Drift, so sub and mids stay phase-aligned through the stack.
- **[2026-04-18] Implemented comprehensive BRAKE slider with quadratic scaling for manual Tape-Stop performance**: APVTS float `BRAKE` (default 1.0) feeds a 50 ms `LinearSmoothedValue`; `brakeRaw²` scales oscillator frequencies and post-envelope / post-gate level before the safety rail for coordinated pitch and level “tape stop” braking.
- **[2026-04-18] Transformed BRAKE slider into a multi-stage Stall Manifold with rate-modulated stuttering**: Below ~0.35 smoothed brake, a square stutter gate (phase accumulator) multiplies the brake multiplier; stutter rate ramps 25 Hz→3 Hz toward stall, duty shrinks for choke; at or below ~0.05 output dies; above the stall threshold behavior is smooth quadratic braking only.
- **[2026-04-18] Redesigned BRAKE as a Mechanical Stress macro with gain-floor protection and rate-modulated gasping stutters**: Smoothed `brakeIn` drives cubic **pitch** pull, `jmap` **gain** (20% floor to full); below 0.6 a free-running square stutter (30 Hz→2 Hz, 50%→5% duty) **replaces** the PULSE/LFO gate while transport phase still advances; below 0.05 full silence.
- **[2026-04-18] Recalibrated BRAKE slider with sqrt-warping and a 20Hz frequency floor for expanded performance resolution**: `warpedBrake = sqrt(brakeIn)` drives cubic pitch and stutter maps; sub frequency is clamped to ≥20 Hz while `brakeIn ≥ 0.1`; stutter engages when `warpedBrake < 0.9` with rate 30→2 Hz and linear `gainMult = brakeIn` (full cut only below the hard stop threshold).
- **[2026-04-18] Implemented Sigmoid input warping and Logarithmic stutter rating for a non-linear, boutique BRAKE feel**: `warpedBrake = 1/(1+exp(-(brakeIn-0.5)*10))` for cubic pitch; stutter depth fades in from raw `0.8→0.6`; below `0.6` stutter rate follows log interpolation **4 Hz→30 Hz** toward the hard stop, with **20 Hz** sub floor for `brakeIn ≥ 0.05`.
- **[2026-04-18] Replaced hard-threshold stutter with a continuous power-curve (1-B)^3 rate and fractional intensity mapping**: `stutterIntensity = 1 - brakeIn`, `stutterRate = 40·(1-brakeIn)³` (0 Hz at full brake, ~0.32 Hz at 0.8, ~40 Hz near zero); smooth gate `1 - intensity·(1 - rawSquare)` multiplies the PULSE gate; sigmoid cubic pitch, linear gain, **20 Hz** sub floor while `brakeIn > 0`.
- **[2026-04-18] Reduced attack pitch thump from +36st to +18st for a more mechanical, less digital impact**: changed `thumpSemitoneFresh` to `18.0f` while keeping the existing fast thump decay timing untouched.
- **[2026-04-19] Implemented Adaptive Release Scaling to prevent pulse-overlap and maintain sub-sonic tightness**: `RumbleEngine` derives `pulsePeriodMs` from BPM × subdivision and caps the exponential note-off τ using the lesser of ~80% of that period and (period − 5 ms) hard silence; the coefficient refreshes each block (and on rate/BPM changes) so fast pulse rates stay punchy instead of smearing into a drone—**independent of GIRTH**, which only shapes the gate blend and spatial/drift behavior.
- **[2026-04-19] Verified LFO/Envelope decoupling to ensure Girth expansion does not affect temporal periodicity**: Renamed the free-running accumulator to `mPulsePhase` (BPM × subdivision only), removed Girth from adaptive release math, locked the first few phase samples of each cycle to the square onset so morph timing cannot “pull” the downbeat, retrigger non-legato `noteOn` from a silent envelope to cut release tails, and added throttled `DBG` logging of `timeBetweenOnsets` in Debug builds to confirm onset spacing is identical at Girth 0 vs 1.
- **[2026-04-19] Implemented Note-Specific Seeded Motif Engine on SKIP slider for deterministic, repeating rhythmic signatures**: Replaced per-pulse random skipping with a 16-bool `mMotifPattern` from `std::mt19937` seeded by `note + (int)(SKIP*100000)`; SKIP sets Bernoulli density (~SKIP fraction of silent steps); the pattern advances each LFO wrap and realigns to step 0 on host bar boundaries (`floor(ppq/4)`); SKIP `0` bypasses to all-true; the motif is rebuilt on non-legato or note-change `noteOn` and when SKIP moves during sustain.
- **[2026-04-19] Refactored RumbleEngine for modularity and added deterministic motif persistence tests.** Details: private nested `MotifEngine` owns SKIP seeding, 16-step pattern, bar anchor, and indexing; `BrakeDynamics` owns BRAKE smoothing, sigmoid pitch, stutter gate, and sub frequency floor; `SignalChain` owns crossover/GIRTH spatial path and DC + safety high-pass. BoutiqueTests cover multi-pair motif persistence, 16-wrap transport with pulse-phase bounds and twin-engine agreement, and host bar-boundary step resets.
- **[2026-04-19] Successfully refactored RumbleEngine into a modular 5-file architecture.** `MotifEngine.h` (SKIP pattern + bar sync), `VoiceBank.h` (three oscillators, HARMONY ratios, sub-drift, thump + note envelope), `BrakePhysics.h` (BRAKE macro smoothing only; rhythmic warp lives in `RumbleEngine` gate), and `SignalProcessor.h` (GRIT manifold + entropy, GIRTH decorrelation/crossover, safety DC/HPF) are header-only peers under `Source/DSP/`; `RumbleEngine.h` orchestrates transport/gate/motif timing and per-sample delegation with no intentional DSP delta vs the pre-split graph.
- **[2026-04-19] Transformed BRAKE from a pitch-dive to a Rhythmic Chaos / Phase-Warping engine.** Implementation: `BrakePhysics` only smooths the macro; oscillators stay on organic VoiceBank Hz. After the strict onset window, `getGateTarget` warps phase with `pow(phase, 1 + 2·(1−brake))` when `brake < 1`; each pulse wrap rolls deterministic skip vs 1/64‑cycle hiccup from a hash of bar × pulse-in-bar × MIDI note; mid-only grit gets a torque addend `(1−brake)·0.55` into the existing manifold.
- **[2026-04-19] Implemented Fractal Stuttering on the BRAKE manifold with phase-locked subdivisions.** When `brake < 0.8`, the gate is multiplied by a 50% duty sub-mask `fmod(pulsePhase·N,1)<0.5` with phase-locked `N` (2|4, 8|16, or 32|64 from brake bands 0.8→0.5, 0.5→0.1, 0.1→0) chosen deterministically from the same `stumbleHash(bar, pulseInBar, note)` as mechanical stumble; motif skips still short-circuit to silence so fractal never fires on SKIP steps.
- **[2026-04-19] Separated SKIP (Silence) and BRAKE (Pulse Fragmentation) logic; removed subtractive muting from BRAKE to ensure SKIP is the only master of gaps.** Gate silence uses only `MotifEngine::pattern[step]` when SKIP is active (`!bypass`); BRAKE no longer applies a full-pulse skip flag, and fractal “shred” is a two-level ratchet (`1.0` vs a `shredValley` floor) so motif-on pulses are never zeroed by BRAKE—only fragmented. Pulse-wrap rolls now only arm deterministic flams (hiccup), not subtractive skips.
- **[2026-04-19] Locked in Additive Ratcheting for BRAKE; ensured SKIP is the sole master of rhythmic gaps.** `fractalThresherGate` blends `(1−brake)²` from clean `1.0` into a phase-locked ratchet (`nChops` 16–128, i.e. 1/16…1/128 of the pulse) with floor **≥0.54**; `BrakePhysics` documents that only SKIP may zero the gate. Torque adds `(1−brake)²·0.15` atop `(1−brake)·0.55` for heavier shred saturation.
- **[2026-04-19] Redesigned GUI into functional groups (Timing, Disorder, Tone) and renamed controls to Fault/Break.** `PluginEditor` now uses three `juce::GroupComponent` sections with grouped horizontal layout: TIMING (`PULSE`, `RATE`, `BPM`), DISORDER (`FAULT`, `BREAK`), TONE (`SHAPE`, `GRIT`, `HARMONY`, `GIRTH`). UI labels were renamed without changing APVTS IDs or slider attachments (`skip_prob`/`brake` remain intact), and `BoutiqueLookAndFeel` rotary accents moved to an industrial palette (charcoal + rust/sulfur).
- **[2026-04-19] Polished UI: Removed BPM buttons, reordered groups to Timing->Tone->Disorder, and fixed keyboard whitespace.** Standalone BPM control now uses direct numeric entry without +/- buttons; editor layout remains a left-to-right signal chain (`TIMING`, `TONE`, `DISORDER`); keyboard range is constrained to C0..C4 with dynamic `setKeyWidth` sizing to fill the full bottom width, and keyboard colours were darkened to match the industrial theme.
- **[2026-04-19] Removed placeholder scaffolding text and expanded keyboard range to C-2 -> C6.** `PluginEditor::paint` now renders only the product title (no temporary “Code-driven -> GUI-driven control” banner), and the keyboard range is set to MIDI `0..96` with `setLowestVisibleKey(0)` plus a minimum key width guard (12 px) to avoid over-squishing while still filling the bottom strip without trailing whitespace.
- **[2026-04-19] Inverted Break logic (0=Clean, 1=Broken), set Fault/Break defaults to 0.0, and finalized keyboard range.** `BrakePhysics` now smooths BREAK directly as `0..1` with `0` as clean baseline; `RumbleEngine` gate/fractal/torque paths consume that value as break intensity so turning BREAK up increases shredding/hiccup aggression. APVTS defaults for `skip_prob` (Fault) and `brake` (Break) were set to `0.0`, state persistence is now handled through APVTS XML `copyState`/`replaceState`, and keyboard range remains `0..96` with full-width dynamic key sizing.
- **[2026-04-19] Finalized UI: Implemented BPM Slider, DAW Sync Toggle (The Light), and inverted Break logic.** BPM now uses a sleek horizontal scrub slider (`40..250`), with a `SYNC` light toggle beside it that routes clocking to host PlayHead when on (and greys/disables manual BPM) or internal tempo when off. Non-standalone builds default SYNC on; standalone defaults SYNC off for immediate manual tempo play. BREAK remains inverted (`0=Clean`, `1=Shredded`), Fault/Break defaults stay at `0.0`, keyboard remains `C-2..C6`, and the scaffolding watermark stays removed.
- **[2026-04-19] Finalized UI: Reordered groups, inverted Break logic, implemented BPM sync, and expanded keyboard.** TIMING->TONE->DISORDER remains the final left-to-right layout; BREAK is locked as `0=Clean` to `1=Shredded`; BPM is now a draggable value box (`40..250`) with SYNC checkbox and tempo-pulsing yellow light driven by the active clock source (host when synced, internal when manual). Keyboard remains full-width at `C-2..C6` (`0..96`), placeholder scaffolding text is removed, and Fault/Break defaults remain `0.0`.
- **[2026-04-19] Implemented strict bypass for Fault engine to eliminate saturation artifacts at 0.00. Optimized branch prediction for bypass state.** In `RumbleEngine::process`, the entire fault manifold now runs only when `faultAmount > 1e-5`; otherwise mids pass through untouched (`// BOUTIQUE RULE: Zero means Zero. Bit-perfect bypass active.`). `SignalProcessor::resetFaultEngine()` clears fault-state internals on active->zero transitions, and fault amount remains driven by the existing `juce::LinearSmoothedValue<float>` path (`mGritSmoothed`) to avoid threshold clicks.
- **[2026-04-19] Removed pitch-bomb/thump logic. Implemented phase-reset on noteOn and micro-envelope (2ms/5ms) to eliminate sine wave transition clicks.** `VoiceBank` now applies direct MIDI base frequency only (no thump semitone envelope), resets all oscillator phases on every `noteOn`, and uses a fixed micro gain envelope with `2 ms` attack and `5 ms` exponential release for clean note transitions.
- **[2026-04-19] Transitioned to Soft Re-trigger logic (1ms fade-out before phase reset) to eliminate sine-wave knocks. Initialized UI Visualizer placeholder.** `VoiceBank` now uses a retrigger state: active notes perform a `1 ms` envelope fade-out, then apply new MIDI frequency + `resetPhase(0.0f)`, then re-enter the `2 ms` fade-in path; pitch-bomb/thump remains removed. Oscillator phase wrap now uses `std::fmod` for robust long-run accumulation, and the editor includes a styled `juce::AudioVisualiserComponent` placeholder panel for waveform validation workflows.
- **[2026-04-19] Tightened Pulse response by reducing de-clicking ramps to 0.1ms/0.2ms. Synchronized phase reset for sample-accurate transient preservation.** `VoiceBank` now uses `0.1 ms` fade-out for soft retrigger and `0.2 ms` fade-in attack, with `resetPhase(0.0f)` occurring on the exact sample where envelope reaches `0.0f`. `RumbleEngine` pulse slew now prioritizes the absolute fastest branch at `PULSE=0.00` (`0.1 ms`), preserving transient snap while maintaining de-click safety.
- **[2026-04-19] Transitioned from Linear to S-Curve (Cosine) amplitude ramps for de-clicking. Reclaimed transient tightness while eliminating slope-change artifacts.** `VoiceBank::tickAmplitudeEnvelope` now drives retrigger fade-out, attack, and release using cosine progress (`0.5*(1-cos(pi*p))`) with short boutique durations (0.5 ms retrigger fade-out, 1.0 ms attack/release). Phase reset remains sample-aligned at the exact fade floor, preserving zero-crossing note starts without linear-ramp knock artifacts.
- **[2026-04-19] Implemented Dual-Oscillator Shadow Crossfading for note transitions. Eliminated phase-reset knocks by allowing the previous note to finish its cycle during the release ramp.** `VoiceBank` now uses two oscillator slots (`active` + `shadow`): on legato `noteOn`, current active is moved to shadow with a 5 ms cosine release, while a freshly reset active slot starts the new note with a 2 ms cosine attack. `sampleOscillators` sums both slots, and shadow voices auto-deactivate at envelope floor to save CPU.
- **[2026-04-19] Added SCULPT section with large ZDF Cutoff knob and global Oscilloscope visualizer.** Added APVTS params for `CUTOFF` (`20..20000 Hz`, skew `0.2`) and `RESO` (`0.1..20`), wired to a post-disorder `juce::dsp::StateVariableTPTFilter<float>` low-pass in `RumbleEngine`. Editor now includes a rightmost `SCULPT` group with large `CUTOFF` + `RESO` knobs and a styled master scope (`juce::AudioVisualiserComponent`) fed from `processBlock` after engine processing.
- **[2026-04-19] Implemented Adaptive Frequency-Dependent Release (0.5/f) to eliminate sub-bass release clicks. Preserved transient tightness for high-frequency pulses.** `VoiceBank` now computes release time as `max(0.002, 0.5 / f)` on note-on and active-frequency refresh, updates `mReleaseStepPerSample` from that dynamic duration for correctly scaled cosine fades, and hard-disables oscillators when envelope reaches absolute zero to prevent denormal tail noise.

## The "Ant" Preferences
- Focus on "Separation" and "Vibe" over clinical phase perfection.
- Support for "Sub-Drift" to create independent movement between the sub and mids.

## May 2026 Updates / Technical Hardening
- **[2026-05-04] Master output gain + tanh ceiling**: Introduced `masterOutputLevel` plus a shape-dependent `tanh` soft-clipper on the final bus (initially −6 dB / fixed drive; later folded into the Volume Normalization pass below). **Reason**: Address “too hot” output from beta testing and avoid digital clipping while keeping a boutique saturation character. **Note**: Output ceiling is lowered without changing DSP phase order or oscillator routing; subsequent passes retune level and clipper drive only.
- **[2026-05-04] Volume Normalization Pass**: SHAPE `jmap` (0.0→1.25, 1.0→0.8); `preGain = masterOutputLevel * shapeGainComp` into shape-dependent `clipperDrive` (higher at sine, lower at saw). Post-clipper grit: `gritCompSquare = 1 - grit²·0.35`, then `finalGritComp = gritCompSquare * sineGritPenalty` where `sineGritPenalty = jmap(min(shape,0.5), 0…0.5, 0.7…1.0)` so low-SHAPE (sine) grit gets up to an extra ~30% attenuation vs saw-range shapes. **`masterOutputLevel` is 0.35** (headroom vs earlier 0.5). **Change**: Lowered `masterOutputLevel` to 0.35 and added `sineGritPenalty` (extra reduction for gritted sines). **Reason**: Digital clipping on high-grit / low-shape combinations; quadratic post-clip grit tame alone was insufficient vs GRIT manifold RMS. **UI**: `AudioVisualiserComponent` set to **2 channels** to match stereo `pushBuffer` from `processBlock` (final buffer after gain chain) for clipping checks at the scope edges.
- **[2026-05-04] Standardized preset path to User Documents**: Preset read/write scanning now targets `userDocumentsDirectory/Rumble/Presets`, and the processor ensures the folder exists via `createDirectory()` when missing. Windows installer preset `DestDir` now matches at `{userdocs}\Rumble\Presets`. **Reason**: Improved user accessibility and sharing for Ant's students, and resolved the mismatch between local testing (Documents) and installer logic (AppData).
- **[2026-05-04] Transitioned to Continuous (Free-Running) Phase**: Removed oscillator `resetPhase(0.0f)` from `VoiceBank::noteOn`/retrigger flow so phase accumulators are not hard-reset on note changes. Added true 25 ms base-frequency glide (`LinearSmoothedValue`) into active voice frequency updates, so note changes move by increment smoothing while phase remains persistent. **Reason**: Eliminated clicks/pops during rapid note changes and keyboard slides. **Note**: Replaces phase-reset-on-noteOn behavior with persistent accumulators for a smoother, more analog legato feel in the Rumble engine.
- **[2026-05-04] Under-the-hood sonic hardening**: Implemented asymmetric saturation in `PluginProcessor` by adding a fixed DC bias before `tanh` and removing the static offset afterward (`tanh(x + bias) - tanh(bias)`), introducing subtle even-harmonic warmth without DC drift. Added dynamic resonance compensation in the SCULPT stage so post-filter output receives a subtle make-up gain as resonance rises. **Reason**: Increase warmth in the grit manifold while keeping low-end weight solid when the filter whistles at higher resonance.
- **[2026-05-04] Deep Chill Normalization Pass**: Widened SHAPE compensation to `1.45x -> 0.65x` (`jmap` over SHAPE 0..1), increased GRIT taming to `gritCompSquare = 1 - pow(grit, 1.8) * 0.50` (up to 50% cut), and added SHAPE/GRIT interaction control `interactionPenalty = 1 - shape*grit*0.15` into `finalBufferGain`. **Reason**: Saw/GRIT perceived loudness remained materially above sine fundamentals; this aggressive mapping targets tighter loudness parity across macro sweeps.
- **[2026-05-04] Post-Clipper "Anchor" Normalization**: Set sine as the unity reference (`shapePenalty` at SHAPE=0 is `1.0`) and moved loudness matching to post-clipper scaling. New flow uses fixed pre-drive `preGain = masterOutputLevel` into `tanh(s * 1.2)` for consistent texture, then applies `finalPostGain = shapePenalty * gritPenalty` where `shapePenalty = jmap(shape, 0..1, 1.0..0.55)` and `gritPenalty = 1 - pow(grit, 1.5) * 0.65`. **Reason**: Pre-clipper compensation was partially collapsed by the tanh ceiling; post-clipper attenuation keeps sine fundamental as the loudest anchor while saw/grit states are level-matched down toward parity.
- **[2026-05-04] Frequency-Dependent Release Hardening**: Tightened `VoiceBank` release-floor logic so envelope release time is never shorter than `1.5 / currentFrequency` seconds (minimum 1.5 waveform cycles at the active pitch), then converted to sample-step as usual. Added a final processor-output DC blocker stage after post-clipper normalization so DC removal is guaranteed at the very end of `processBlock`. **Reason**: Eliminated residual sine clicks by enforcing long-enough low-frequency release windows and ensuring final-stage offset cleanup stays last in the chain.
- **[2026-05-04] Filter Parameter Smoothing**: Implemented `juce::LinearSmoothedValue<float>` ramps for SCULPT `Cutoff` and `Resonance` with a ~30 ms transition. Filter coefficients are refreshed in the audio loop every 8 samples from the smoothed values, and the existing resonance make-up gain (`1 + resonance * 0.05`) now uses the smoothed resonance path as well. **Reason**: Eliminated zipper noise / click artifacts during live cutoff-resonance moves, especially audible at high resonance around ~110 Hz.
- **[2026-05-05] SCULPT tail hard-cut removal + silence-gated reset**: Removed immediate zeroing of output when note envelope reaches 0 while cutoff/resonance filter state may still be ringing. Added a short post-note holdoff (~20 ms) and only reset safety/filter state after output magnitude falls below a small silence threshold. **Reason**: Prevented release-end clicks caused by discontinuities when resonant filter tails were being truncated near low-cutoff regions (e.g. ~150 Hz).
- **[2026-05-05] Legato tests + coverage expansion**: Confirmed design intent: **no oscillator phase reset on legato** (`VoiceBank` keeps continuous phase). Updated Catch2 tests to compare phases before/after legato `noteOn` and to assert frequency glide (trace starts near prior note, ends near target). Added direct suites for `MotifEngine`, `SignalProcessor` (taper, grit bypass, DC safety), and `BrakePhysics`; relaxed `noteOff` silence upper bound to accommodate SCULPT/filter tail decay (~3k samples at 44.1 kHz in current graph).
- **[2026-05-06] Fully automated macOS Signing & Notarization**: CI on `macos-latest` now imports Developer ID Application + Installer certificates from secrets, signs plugin bundles with hardened runtime entitlements, builds/signs a macOS installer package, submits it for notarization, and staples the approved ticket before artifact/release upload. **Reason**: To comply with Apple Gatekeeper requirements for the 130-student rollout. **Result**: Builds on `macos-latest` now produce a trusted, notarized `.pkg` installer.

## Current Technical Debt / Next Steps
- Monophonic MIDI note-stack / last-note priority is implemented in `PluginProcessor`; remaining polish is optional (e.g. channel pressure, sustain CC semantics).
- `Base 5` Macros are functionally complete.